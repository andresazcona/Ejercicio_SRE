global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # El OTel Collector expone las métricas OTLP convertidas a formato Prometheus
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8889']
